export enum SocketActions {
  JOIN_ROOM,
  LEAVE_ROOM,
  UPDATE_PEOPLE_COUNT,
  LOAD_CANVAS,
  CANVAS_ACTION,
}

export enum CanvasActions {
  OBJECT_ADDED,
  OBJECTS_REMOVED,
  OBJECT_MODIFIED,
  SEND_OBJECT_FORWARD,
  SEND_OBJECT_BACKWARD,
  UNDO_ACTION,
}
